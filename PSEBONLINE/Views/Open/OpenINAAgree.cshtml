@model PSEBONLINE.Models.SchoolModels
@{
    Layout = "~/Views/Shared/_Headerui.cshtml";
}

@using (Html.BeginForm("OpenINAAgree", "Open", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <h1>
       Open INA Guidelines For Examination - Session @Session["Session"].ToString()
    </h1>
    <div class="row">
        <div class="col-md-9">
            <h4>
                Instructions
            </h4>
        </div>
        <div class="col-md-3 text-right"> <a class="btn" href="~/PDF/OpenINAMarks.pdf" target="_blank">Download/Print Instructions</a></div>
        <div class="clearfix"></div>
        <div class="col-md-12">
            @if (Session["OpenINAAgree"] != null)
            {
                if (Session["OpenINAAgree"].ToString() == "S")
                {
                    <iframe src="../../PDF/Open_INA_Agree.pdf" class="col-lg-12 col-md-12 col-sm-12 iframeh"></iframe>
                }
                else if (Session["OpenINAAgree"].ToString() == "M")
                {
                    <iframe src="../../PDF/Open_INA_Agree.pdf" class="col-lg-12 col-md-12 col-sm-12 iframeh"></iframe>
                }              
            }

        </div>
        <div class="clearfix"></div>
        <div class="col-md-12">
            <p>
                @Html.RadioButtonFor(model => model.Agree, "Agree", new { id = "Agree", @checked = "true" }) <span class="kdsfont">Ìè× ÓÚÕÌ¼ ÕÙ </span> (I Agree)
                @Html.RadioButtonFor(model => model.Agree, "NotAgree", new { id = "NotAgree", }) <span class="kdsfont">Ìè× ÓÚÕÌ¼ ÅÕÛ× ÕÙ</span> (I do not Agree)
            </p>
        </div>
        <div class="clearfix"></div>
        <div class="col-md-12 text-center">
            <input type="submit" id="Submit" value="Submit" />

            <a href="/Open/Open_INA_Portal" class="btn">Back</a>
        </div>

    </div>

}